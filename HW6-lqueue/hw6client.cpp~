#include<iostream>
#include<iomanip>
#include"lqueue.h"
#include<cstdlib>
using namespace std;



int main()
{
  cout<<"Case 1:"<<endl;
  lqueue L;
  cout<<"1.";if(L.isEmpty())
    {
      cout<<"The queue is empty.";
      cout<<endl;
    }
  else
    {
      cout<<"The queue is NOT empty.";
      cout<<endl;
    }
  cout<<"2.";cout<<"Displayng queue: "; L.displayAll();cout<<endl;
  cout<<"3.";cout<<"adding integers..."; L.addRear('1');L.addRear('2');L.addRear('3');cout<<endl;
  cout<<"4.";cout<<"Displaying queue: "; L.displayAll();cout<<endl;
  el_t X;
  cout<<"5.";
  L.deleteFront(X);cout<<"deleting "<<X<<",";L.deleteFront(X);cout<<"deleting "<<X;cout<<endl;
  cout<<"6.";cout<<"Displaying queue: ";L.displayAll();cout<<endl;
  cout<<"7."; if(L.isEmpty())
    {
      cout<<"The queue is empty";
      cout<<endl;
    }
  else
    {
      cout<<"The queue is NOT empty.";
      cout<<endl;
    }
  cout<<"8."; L.deleteFront(X);cout<<"deleting "<<X;cout<<endl;
  cout<<"9.";if(L.isEmpty())
    {
      cout<<"The queue is empty.";
      cout<<endl;
    }
  else
    {
      cout<<"The queue is NOT empty.";
      cout<<endl;
    }
  cout<<endl;
 cout<<"Case 2:"<<endl;
 lqueue L2;
 cout<<"1."; L2.displayAll();cout<<endl;
 cout<<"2."; L2.deleteFront(X);cout<<X;cout<<endl;
}
